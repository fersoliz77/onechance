{
  "rules": {
    "userRoles": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'admin' || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'super_admin' || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
        ".write": "auth != null && ((auth.uid === $uid && !data.exists() && newData.child('systemRole').val() === 'user') || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'super_admin' || auth.token.role === 'super_admin')"
      }
    },
    "profiles": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'admin' || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'super_admin' || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'admin' || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'super_admin' || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
      }
    },
    "videos": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'admin' || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'super_admin' || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'admin' || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'super_admin' || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
      }
    },
    "photos": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'admin' || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'super_admin' || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'admin' || root.child('userRoles/' + auth.uid + '/systemRole').val() === 'super_admin' || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
      }
    }
  }
}
